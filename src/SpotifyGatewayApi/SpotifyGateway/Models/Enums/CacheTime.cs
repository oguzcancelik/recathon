namespace SpotifyGateway.Models.Enums
{
    public enum CacheTime
    {
        OneMinute = 1,
        TenMinutes = 10,
        HalfHour = 30,
        _58Minutes = 58,
        _59Minutes = 59,
        OneHour = 60,
        ThreeHours = 3 * 60,
        TenHours = 10 * 60,
        OneDay = 24 * 60,
    }
}